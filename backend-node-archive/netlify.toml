[build]
  publish = "public"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Pretty API paths: https://<site>/api/<fn> -> function
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
